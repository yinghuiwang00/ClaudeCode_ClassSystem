# ======================
# Docker Compose 环境变量配置
# ======================

# ======================
# JWT 配置
# ======================
# 生产环境必须修改此密钥！
JWT_SECRET=your-secure-jwt-secret-key-minimum-64-chars
JWT_EXPIRATION=86400000  # 24小时，单位毫秒

# ======================
# 数据库配置 (选择一种模式)
# ======================

# 模式1: H2 文件数据库 (默认)
# 使用默认的 docker-compose.yml 配置即可

# 模式2: PostgreSQL (取消注释并配置)
# SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/bookingdb
# SPRING_DATASOURCE_USERNAME=postgres
# SPRING_DATASOURCE_PASSWORD=postgres
# SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver
# SPRING_JPA_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect

# ======================
# 应用配置
# ======================
SPRING_JPA_SHOW_SQL=true
SPRING_PROFILES_ACTIVE=  # 可设置为 production, development 等

# ======================
# 日志配置
# ======================
LOGGING_LEVEL_COM_BOOKING_SYSTEM=DEBUG
LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY=DEBUG

# ======================
# JVM 配置
# ======================
JAVA_OPTS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -Xmx512m -Xms256m

# ======================
# 其他配置
# ======================
SERVER_PORT=8080